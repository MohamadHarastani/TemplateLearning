conda create -y -n TemplateLearning python=3.9
conda activate TemplateLearning
pip install ProDy tqdm mrcfile scikit-image pyfftw pip install python-parakeet==0.4.5 h5py==3.8.0
# you need to have eman2 installed, and install yaml on it
conda activate eman2-env  # get the name of the environement from "conda env list"
pip install pyyaml

